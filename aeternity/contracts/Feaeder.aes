@compiler >= 6

contract Feaeder =
  record state = {
    controller: address,
    allowedList
    }

  datatype event =
    AggregatorOracleEvent(address, int) |
    VRFRequestEvent(int, address)

  stateful entrypoint init() = { controller = Call.caller }

  

